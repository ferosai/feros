[package]
name = "common"
version = "0.2.0"
edition = "2021"
description = "Shared data types for the voice engine (cross-crate configs, enums)"

[dependencies]
serde = { version = "1", features = ["derive"] }
